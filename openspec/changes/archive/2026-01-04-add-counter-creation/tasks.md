## 1. Implementation
- [x] 1.1 Define domain model for Counter with UUID identifier, name, and unit
- [x] 1.2 Add persistence schema (Postgres) for counters including UUID primary key, name/unit fields, and a uniqueness constraint on name
- [x] 1.3 Implement API endpoint to create a counter (returns UUID)
- [x] 1.4 Implement minimal UI flow to create a counter
- [x] 1.5 Add validation and error handling for invalid inputs (empty name/unit, invalid unit format, etc.)

## 2. Validation
- [x] 2.1 Map each spec scenario to at least one automated test (unit/integration)
- [x] 2.2 Run `openspec validate add-counter-creation --strict` and fix all issues


